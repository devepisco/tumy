const { structure } = require("../../middlewares/utils")
const canceledService = require("../../models/canceledServices")

const driverCancelService = structure( async (req, res) => {
    const canceledService = new canceledService({

    });
});
module.exports ={driverCancelService}